# Optional. The GA4 measurement ID is hardcoded in src/components/Analytics.astro
# (it is public by design). Set this only to point a build at a different
# property — e.g. a staging property you do not want mixed into the real one.
PUBLIC_GA_ID=
